use my_service_bus_shared::protobuf_models::MessageProtobufModel;

use crate::app::AppContext;

pub fn new_messages(app: &AppContext, topic_id: &str, messages: &[MessageProtobufModel]) {
    todo!("Implement")
}
